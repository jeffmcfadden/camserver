%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{ data: {toggle: "collapse", target: "#bs-example-navbar-collapse-1" }}
        %span.sr-only
          Toggle Navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{ href: "/" }
        CAMSERVER
      
    .collapse.navbar-collapse#bs-example-navbar-collapse-1
      - if current_user.present?
        %ul.nav.navbar-nav
          %li
            = link_to "All Events", [:motion_events]
          %li
            = link_to "Cameras", [:cameras]
          %li
            = link_to "Calendar", calendar_motion_events_path
          %li
            = link_to "Sidekiq", "/sidekiq"
    
      %ul.nav.navbar-nav.navbar-right
        - if current_user.nil?
          %li
            = link_to "Sign In", new_user_session_path
        - else
          %li
            = link_to "Sign Out", destroy_user_session_path, method: :delete