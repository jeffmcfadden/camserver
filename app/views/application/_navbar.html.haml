%nav.navbar.navbar-expand-lg.navbar-light.bg-light
  %a.navbar-brand{ href: "/" }
    CAMSERVER
  
  %button.navbar-toggler{ type: "button", data: { toggle: "collapse", target: "#navbarContent" } }
    %span.navbar-toggler-icon
    
  .collapse.navbar-collapse#navbarContent
    - if current_user.present?
      %ul.navbar-nav.mr-auto
        %li.nav-item
          = link_to "All Events", [:motion_events], class: "nav-link"
        %li.nav-item
          = link_to "Cameras", [:cameras], class: "nav-link"
        %li.nav-item
          = link_to "Calendar", calendar_motion_events_path, class: "nav-link"
        %li.nav-item
          = link_to "Sidekiq", "/sidekiq", class: "nav-link"
  
    %ul.nav.navbar-nav.navbar-right
      - if current_user.nil?
        %li.nav-item
          = link_to "Sign In", new_user_session_path, class: "nav-link"
      - else
        %li.nav-item
          = link_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link"